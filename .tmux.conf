# ============================================================================
# Tmux Configuration (.tmux.conf)
# ============================================================================
# Configuration for tmux terminal multiplexer

# Use Ctrl-a as the prefix key (instead of default Ctrl-b)
# This is more convenient and matches GNU Screen's default
set -g prefix C-a
