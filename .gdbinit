# ============================================================================
# GDB Configuration (.gdbinit)
# ============================================================================
# Configuration for GDB (GNU Debugger)

# Save command history across sessions
set history save on

# Keep unlimited history
set history size unlimited

# Don't use shell when starting programs (for security and consistency)
set startup-with-shell off
